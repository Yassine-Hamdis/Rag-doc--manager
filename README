open your terminal :

cd frontend
npm install
npm run dev


new terminal :

cd backend 
pip install -r requirements.txt
uvicorn app.main:app --reload   

or use docker-compose (Recommended) and run with :
docker compose up --build